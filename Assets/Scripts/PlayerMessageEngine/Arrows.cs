using System;

namespace AssemblyCSharp
{
	// Contains each arrow that can possibly show up
	// each arrow must have an equally named picture file in the resources folder
	public enum Arrows
	{
		ArrowForward, ArrowLeft, ArrowRight
	}
}

